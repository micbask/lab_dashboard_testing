# scripts/email_ingest.py uses only stdlib beyond what the repo's main
# requirements.txt already provides (pandas, pyarrow, openpyxl, xlrd,
# duckdb, requests, streamlit). Mail fetching is handled by AppleScript
# via /usr/bin/osascript, so no extra Python deps are needed.
