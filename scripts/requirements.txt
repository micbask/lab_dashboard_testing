# scripts/email_ingest.py — standalone dependencies
#
# Install with:
#     pip install -r requirements.txt
#
# The script uses only these + the Python stdlib.  Mail fetching is
# delegated to Apple Mail via /usr/bin/osascript, so no email client
# library is needed.

pandas>=2.0.0
pyarrow>=15.0.0
openpyxl>=3.1.0
xlrd>=2.0.0
requests>=2.31.0
